// Note: Schedule page is not currently in use; keeping file for potential future re-enable
import { PageLayout } from "@/components/shared/PageLayout"
import { SchedulePage } from "@/components/SchedulePage"

export default function ScheduleRouterPage() {
  return (
    <PageLayout>
      {/* Schedule is disabled/not in use */}
      {/* <SchedulePage /> */}
    </PageLayout>
  )
}
